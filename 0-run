#!/bin/bash
# Shell script to run a Python script from the $PYFILE environment variable

# Check if the PYFILE variable is set
if [ -z "$PYFILE" ]; then
  echo "Error: PYFILE environment variable is not set."
  exit 1
fi

# Execute the Python script
python3 "$PYFILE"
